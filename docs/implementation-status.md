# 実装状況（生きたタスクリスト）

**ここには「いま生きているもの」だけを置く。** 済んだら行ごと消す。
更新するのは親セッションだけ。

---

## 🚦 いま動いているもの

（なし。第1波は 2026-09-19 に完了 → `docs/changelog/2026-09.md`）

---

## 既知の不具合

| # | 場所 | 症状 | 方針 |
|---|---|---|---|

## 未実装（Phase 1 MUST）

（なし。残るは実機での動作確認 → 下の「ユーザー作業待ち」）

## 未実装（Phase 2 SHOULD）

| ID | 要件 |
|---|---|
| R-05 | 振込手数料などの期間費用（`expense` テーブルは済、API・UIが無い） |
| — | 収集の `scrape()` セレクタ検証（実DOMを見ないと直せない） |

## ユーザー作業待ち

- **実機で第1波の動作確認**: `pnpm dev` → 仕入登録（送料100・数量3で在庫が 1033/1033/1034 になること）→ 売上の手入力 → 紐付け → 解除 → 自家消費。エージェントは Electron を起動していないので、画面は未確認
- **メルカリにログインして `scrape()` のセレクタを検証**（DevTools で実DOMを見る）
- git 初期化と初期コミット（親が行う。ユーザーの OK 待ち）

## 📝 メモ（ユーザー記入欄）


---

## 第2波（候補・未着手）

第1波のレビューで見つかった積み残しをここに足してから着手する。

| WS | 対象ファイル | 内容 |
|---|---|---|
| WS-A | `src/main/db.ts` `src/shared/types.ts` `src/preload/index.ts` `src/main/index.ts` | R-05 期間費用: `listExpenses(month?)` / `createExpense` / `deleteExpense`。`monthly_summary` に期間費用を足すか、別ビュー `monthly_expense` にして画面で並べる（**利益の式は変えない**。粗利と純利益を分けて出す） |
| WS-B | `src/renderer/views/Settings.vue` | 期間費用の入力UI（振込手数料など。日付・区分・金額） |
| WS-C | `src/renderer/views/Inventory.vue` | 月次タブに期間費用・純利益の列を追加 |
| — | `src/main/collector.ts` | `scrape()` のセレクタ検証。**実際にログインした状態の DOM を見ないと直せない**のでユーザー作業が要る |
| — | リポジトリ | まだ `git init` していない。第1波の統合時に初期化してコミットする |
