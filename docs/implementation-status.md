# 実装状況（生きたタスクリスト）

**ここには「いま生きているもの」だけを置く。** 済んだら行ごと消す。
更新するのは親セッションだけ。

---

## 🚦 いま動いているもの

（なし。第1波・第2波・Phase 2（型番で紐付ける）は 2026-09-19 に完了 → `docs/changelog/2026-09.md`）

---

## 既知の不具合

| # | 場所 | 症状 | 方針 |
|---|---|---|---|

## 未実装（Phase 1 MUST）

（なし。残るは実機での動作確認 → 下の「ユーザー作業待ち」）

## 未実装（Phase 2 SHOULD）

| ID | 要件 |
|---|---|
| R-05 | 振込手数料などの期間費用（`expense` テーブルは済、API・UIが無い） |
| — | 収集の `scrape()` / `scrapeDetail()` セレクタ検証（実DOMを見ないと直せない） |
| P-07 | メロジョイの注文履歴（Shopify）から注文番号・価格・送料を取って下書きに突合（`collector-mellojoy.ts`、実DOM待ち） |

## ユーザー作業待ち

- **実機で第1波＋第2波（新しい画面）の動作確認**: `pnpm dev` → 仕入登録（送料100・数量3で在庫が 1033/1033/1034 になること）→ 売上の手入力 → 発送方法を選ぶ → 紐付け（右ドロワー）→ 解除 → 自家消費。画面は隠しウィンドウのスクリーンショットでしか確認していない
- **メルカリにログインして `scrape()`（売却済み一覧）と `scrapeDetail()`（取引詳細：販売手数料・送料・説明文）のセレクタを検証**（DevTools で実DOMを見る）
- **メロジョイに1アカウントでログインし、注文履歴の一覧と1注文の詳細の DOM を見せる**（P-07 の前提）
- **設定タブで「メロジョイの取り込み先アカウント」を選び、仕入タブの「メロジョイの購入記録を取り込む」を実行**して下書きが積まれることを確認

## 📝 メモ（ユーザー記入欄）


---

## 第3波（候補・未着手）

第1波のレビューで見つかった積み残し。第2波（画面の再設計）は完了。

| WS | 対象ファイル | 内容 |
|---|---|---|
| WS-A | `src/main/db.ts` `src/shared/types.ts` `src/preload/index.ts` `src/main/index.ts` | R-05 期間費用: `listExpenses(month?)` / `createExpense` / `deleteExpense`。`monthly_summary` に期間費用を足すか、別ビュー `monthly_expense` にして画面で並べる（**利益の式は変えない**。粗利と純利益を分けて出す） |
| WS-B | `src/renderer/views/Settings.vue` | 期間費用の入力UI（振込手数料など。日付・区分・金額） |
| WS-C | `src/renderer/views/Monthly.vue` | 月次に期間費用・純利益の列を追加 |
| — | `src/main/collector.ts` | `scrape()` のセレクタ検証。**実際にログインした状態の DOM を見ないと直せない**のでユーザー作業が要る |
| — | `src/renderer/views/{Dashboard,Sales}.vue` ほか | 正の粗利にも「＋」を付けるか（PRODUCT.md は「符号でも読める」と書いているが、現状は負にだけ「−」）。ダークモード（トークンは意味名なので `prefers-color-scheme` 層を足せる）|
