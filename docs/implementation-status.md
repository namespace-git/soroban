# 実装状況（生きたタスクリスト）

**ここには「いま生きているもの」だけを置く。** 済んだら行ごと消す。
更新するのは親セッションだけ。

---

## 🚦 いま動いているもの

（なし。2026-09-20 深夜〜早朝の自走分（期間費用・純利益・按分の整数統一・出品タブの仕上げ・横断検索・引き当ての移動）は完了 → `docs/changelog/2026-09.md`。Codex による main 側コードレビューは Codex の利用上限で未実施 → 上限が戻ったら再依頼）

---

## 既知の不具合

| # | 場所 | 症状 | 方針 |
|---|---|---|---|

## 未実装（Phase 1 MUST）

（なし。残るは実機での動作確認 → 下の「ユーザー作業待ち」）

## 未実装（Phase 2 SHOULD）

| ID | 要件 |
|---|---|
| — | 収集の `scrape()` / `scrapeDetail()` セレクタ検証（実DOMを見ないと直せない） |

## ユーザー作業待ち

- **実機で第1波＋第2波（新しい画面）の動作確認**: `pnpm dev` → 仕入登録（送料100・数量3で在庫が 1033/1033/1034 になること）→ 売上の手入力 → 発送方法を選ぶ → 紐付け（右ドロワー）→ 解除 → 自家消費。画面は隠しウィンドウのスクリーンショットでしか確認していない
- **出品中タブの取り込みを実機で 1 回回す**：出品タブに出品が並ぶこと、キーワード不一致が入らないこと、要対応に「未引き当ての出品」が出ること。引き当て → 売れたときに販売へ引き継がれること
- **メルカリにログインして `scrape()`（売却済み一覧）と `scrapeDetail()`（取引詳細：販売手数料・送料・説明文）のセレクタを検証**（DevTools で実DOMを見る）
- **取り込みをもう1回回す**（ヘッダの「取り込む」）：既知の 3 注文に到着状態（未発送／配送中）が付くこと、売上の行にサムネイルが出ること（新規＋既知の未保存分、最大 30 枚／回）
- **配達中の注文の詳細ページの HTML**を見せてもらえれば、発送予定日・追跡番号が取れるか判断できる
- **メルカリの取引中タブ（`/mypage/listings/in_progress`）と取引画面 1 つ（`/transaction/m…`）の HTML** → 発送・受取・取引完了・買い手を履歴に載せる（H-03）
- メロジョイの商品画像：注文画面には無いので、ストアの商品検索 JSON（`/search/suggest.json`）で型番から引く案。やるかどうか要相談

## 📝 メモ（ユーザー記入欄）


---

## 第3波（候補・未着手）

第1波のレビューで見つかった積み残し。第2波（画面の再設計）は完了。

| WS | 対象ファイル | 内容 |
|---|---|---|
| — | `src/main/collector.ts` | `scrape()` のセレクタ検証。**実際にログインした状態の DOM を見ないと直せない**のでユーザー作業が要る |
| — | `src/renderer/views/{Dashboard,Sales}.vue` ほか | 正の粗利にも「＋」を付けるか（PRODUCT.md は「符号でも読める」と書いているが、現状は負にだけ「−」）。ダークモード（トークンは意味名なので `prefers-color-scheme` 層を足せる）|
