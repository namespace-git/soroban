# 2026-09

出荷ずみ（統合ずみ）の実装記録。1件あたり数行で「何を・なぜ・どう確かめたか」。

## 2026-09-19 第1波 — Phase 1 の穴埋めと金額バグ修正

- **按分の1円消失を修正**（`src/main/money.ts` `splitEvenly`）。明細内のアイテムに配賦額を割るとき、余りを最後の数点に +1 して Σlanded_cost が「明細合計＋配賦額」と必ず一致するようにした。`listPurchases.total_cost` も同じ式に。テストで 100円/3点 → 1033/1033/1034 を確認
- **日付を JST に統一**（`src/shared/date.ts`）。ダッシュボードの月キー・収集の販売日・仕入フォームの初期値が UTC でずれていた
- **候補提示の正規化**（`suggestInventory`）を JS 側に寄せ、販売タイトルと在庫名に同じ正規化を掛けるようにした
- **販売の手入力**（S-02）、**紐付けの解除・付け替え**（M-05, `listSaleLines` 追加）、**梱包材費**（S-07）、**自家消費**（I-05, `disposeInventory` に status）を追加
- 紐付けパネルの粗利プレビューが `packaging_cost` を引いていなかったのを修正
- **テスト基盤**: vitest 3（vite 5 対応）。`better-sqlite3` が Electron ABI なので `pnpm test` は `ELECTRON_RUN_AS_NODE=1 electron vitest` で回す。16件
- 環境: pnpm 10 の build script 許可（`pnpm.onlyBuiltDependencies`）、`tsconfig` に `vite/client`、`@types/better-sqlite3`

確認: `pnpm typecheck` / `pnpm build` / `pnpm test` すべて緑。**画面は実機未確認**（エージェントは Electron を起動しない方針）

## 2026-09-19 第2波 — 画面の再設計（業務SaaS標準型）

- **見た目を全面的に置き換えた。** 方針は「業務SaaSの標準型」、品質基準はマネーフォワード クラウド（`PRODUCT.md` Brand Commitments、`.impeccable/surfaces/src-renderer-app-vue.md` に契約）。ライト。寒色グレー地＋白面＋1px罫、ブランド青は主ボタン・選択・フォーカスだけ、利益=緑／損失=赤／要対応=琥珀は数字とチップに限定。トークンと部品の記録は `DESIGN.md` / `.impeccable/design.json`
- **構成の変更**: 左サイドナビ（220px、1100px 未満でアイコン帯）＋上部バー。月次を在庫のサブタブから独立ページ `Monthly.vue` へ。紐付けはモーダルをやめ右ドロワー（`components/Drawer.vue`、背後にテーブルが見える）。売上ナビに未処理件数バッジ（`listSales({onlyPending})` の件数。子 view は `inject('changed')` で更新を通知）
- **共通部品**: `components/{Icon,Drawer,StatusChip,EmptyState,Skeleton}.vue`。アイコンは自前 SVG 20種（絵文字・記号を廃止）。`style.css` は全面置換（クラス語彙は `DESIGN.md`）
- **売上タブ**: 行 49px（チップは商品名の右）、発送方法セレクトのラベルを「名前　¥送料」に、未確定の select は琥珀地、発送方法を選ぶと粗利セルが 180ms で入れ替わる。ドロワーの候補行は 32px ピッチ（30〜100点向け）。操作数は変えていない
- **負値の表記**を `−¥1,234` に統一（Dashboard / Sales）
- **開発用モック** `src/renderer/mock/soroban-mock.ts`: `import.meta.env.DEV && !window.soroban` のときだけ動的 import。素のブラウザで全画面を動かせる（`.claude/launch.json` の `renderer` = `electron-vite dev --rendererOnly`）。本番バンドルには入らないことを `pnpm build` の出力で確認
- **レビュー**: impeccable の detector `[]`、finish-reviewer 2ラウンド（8件＋3件を修正）。最終ラウンド後に残った2件（設定の送料セルの `¥` の間隔、1366px での発送方法ラベルの省略）は親が幅とラベル形式を直し、スクリーンショットで確認。**reviewer の再採点は受けていない**
- **ロジックは変えていない**: `src/main/` `src/preload/` `src/shared/` は無変更。文言も現状踏襲（新規は「実行履歴」「全件を表示するには…」の2つ）

確認: `pnpm typecheck` / `pnpm build` 緑。画面は隠し Electron ウィンドウで 1366×768 と 1024×700 を撮影して確認（`.impeccable/review/`、git 対象外）。**実機（`pnpm dev`）での操作確認は未実施**

## 2026-09-19 Phase 2 — 型番で紐付ける

- **契約・DB**（`shared/types.ts` `main/schema.sql` `main/db.ts` `main/code.ts`）：`model_code` / `series_code` / `material` を仕入明細と在庫に。`【Z078-2】` の抽出は `code.ts`（バリアント名に【】があればそれ、なければ商品名側。`Z048-10` を「Z048 の 10 番」と解釈しない）。マイグレーションは `schema_version` で管理し、`inventory_item` は CHECK を変えるためテーブル再構築
- **自動紐付け**：枝番まで完全一致した販売用だけ、`acquired_at` の古い順に1点を `link_source='auto'` で確定（「自動確定しない」の唯一の例外）。シリーズ一致・複数型番・型番なしは候補提示のまま。`autoLinkPending()` で溜まった分を一括
- **分割**（ばらし売り）：`splitInventory` — 原価を等分し端数は最後の子、親は `split`。**過去の原価は動かない**
- **下書き仕入**：`mellojoy-watch` の `meta.json` から商品・型番・数量を積む（価格なし）。`confirmPurchase` で単価と送料を入れて在庫を生成。同じ記録は二度積まない（`import_key`）
- **販売実額**：`applySaleActuals` で手数料・送料を `shipping_source='actual'` として確定。収集は未入力の販売だけ詳細を開く（**セレクタ未検証**）。説明文の型番は `appendModelCodes` で追記し、取り込み直後の「私物」は型番が見つかれば販売用に戻す
- **私物判定**：キーワード未設定なら「型番があれば販売用、なければ私物」として取り込む
- **収集の振る舞い**：普通の Chrome の UA と `Accept-Language`、2〜6 秒のランダム待ち、1回 6 ページまで、既定 1 時間、CAPTCHA・本人確認で即停止
- **画面**：売上（自動チップ・実額表示・型番で自動紐付け・メモ）、在庫（型番チップ・分割・型番の手修正・メモ・分割済みフィルタ）、仕入（購入記録の取り込み・下書きの確定フォーム・型番プレビュー・メモ・仕入先の種別）、ホーム（価格未入力の仕入・型番ランキング）、設定（キーワード・記録フォルダ・取り込み先アカウント）
- テスト 77 件（按分・型番抽出・自動紐付け・分割・下書き→確定・マイグレーション・UA・CAPTCHA 判定・meta.json 読み込み）

確認: `pnpm typecheck` / `pnpm test` 緑。画面は隠しウィンドウのスクリーンショットで確認。**実機での取り込み（メルカリ詳細・メロジョイ注文履歴）は DOM 未確認のため未検証**

## 2026-09-19 実機フィードバック対応（Phase 2 続き）

- **`window.prompt()` は Electron で動かない**（「仕入先を追加」「発送方法を追加」「メモ」「分割」「型番」が無反応だった原因）。`components/InputDialog.vue` を作り、`App.vue` が `provide('prompt', ask)`。全画面の `prompt()` を置き換え
- **販売履歴ページを収集の正にした**（`/mypage/listings/sold`）。商品・価格・販売手数料・送料・購入完了日が表で取れるので、詳細ページを実額のために開かない。既知の販売も実額と本当の販売日で更新（`updateCollectedActuals`）。実DOMの抜粋を `__tests__/fixtures/mercari-sold.html` に保存。ページネーションは未対応
- **CAPTCHA 誤検知を修正**：マイページ本文に「本人確認」が普通に含まれる。URL・CAPTCHA iframe・短い本文＋キーワード に限定
- **キーワードを複数**（`,` `、` 空白区切り、どれか含めば販売用）
- **mellojoy-watch 連携を撤去**（ユーザー指示）。下書き→確定の仕組みは注文履歴用に残す
- **メロジョイのログイン窓**（`collector-mellojoy.ts`、アカウント別 `persist:mellojoy-<id>`）。設定タブの「仕入先」からログイン。注文履歴の取得は DOM 未確認で未実装
- **設定タブを再構成**：手数料／在庫／取り込み／メルカリ（ログイン・キーワード）／仕入先（一覧・ログイン）／データ。ヘッダーの「メルカリにログイン」を撤去。振込手数料は「月1回の振込として月次に計上」の前提（R-05 は未実装）
- テスト 82 件
- **日本語入力の確定 Enter でダイアログが閉じていた**（「仕入先に文字が打てない」の原因）。IME 変換中（`isComposing` / keyCode 229）は反応しないように
- **取引データの初期化**（設定 → データ）：販売・仕入・在庫・紐付け・取り込み履歴・期間費用を消す。設定・仕入先・発送方法は残す。「初期化」と入力して確定
- **仕入先の登録を設定タブへ**（仕入タブのボタンは撤去）。認証は Cookie（ログイン状態）のみ保存、パスワードは保存しない方針を再確認（2026-09-19）

## 2026-09-19 自動取得の判別とタグ

- **自動取得の印**：販売の `source`（collector / manual）を画面に出す。売上タブの販売日の下に「自動取得」。紐付けの自動確定は「自動紐付け」に改名して区別
- **タグ**：`tag` / `sale_tag` / `inventory_tag`（schema_version 3）。設定タブで登録・改名・削除、売上・在庫の行の「タグ」からポップオーバー（`components/TagPicker.vue`）で付け外し（即時反映、その場で新規作成も可）。売上タブでタグ絞り込み → 件数・売上・手数料・送料・原価・粗利の合計を DB 側で集計（`saleTotals`）。CSV にも「取得元」「タグ」列
- テスト 89 件。実 DB のコピーで v1→v3 のマイグレーションを確認
- **仕入先の改名・種別変更・無効化・削除**（設定 → 仕入先）。仕入で使われている仕入先は消せず「無効にする」。削除時はログイン用プロファイルも消す（`clearShopSession`）

## 2026-09-20 メロジョイの注文履歴を取り込む（P-07）・到着状態・画面の幅とメモ

- **注文履歴の取り込み**（`collector-mellojoy.ts`）：Shopify 新カスタマーアカウントの注文一覧（`article[aria-labelledby="order-#…"]`）と注文詳細（`role="table"` の明細・注文合計）を実DOMで確認し、`__tests__/fixtures/mellojoy-orders.html` / `mellojoy-order.html` に保存。「取り込む」と起動時の自動取り込みで、メルカリの後に有効な仕入先アカウントを直列で読む（一覧1ページ＋詳細最大5件／回、2〜6秒待ち）。明細の単価が整数で決まり **小計＋配送−割引＝合計** が一致すれば確定して在庫を作る。一致しなければ取れた値を入れた下書き（`draft`）。明細が読めなければ何も作らず次回に回す。キャンセル済みは飛ばす。`import_key = mellojoy:#注文番号`。注文日は「確認日: 9月19日」（年なし）から推定。実機で 3 注文が確定（合計一致を確認）、キャンセル 1 件を除外
- **取り込みの記録**：`collector_run` に `source`（mercari / mellojoy）と `shop_account_id`（schema_version 4）。`collect()` は 1 回分の配列を返し、トーストは「メルカリ: …／アカウント名: …」にまとめる。設定の実行履歴に「対象」列
- **到着状態**（schema_version 5）：`purchase.fulfillment`（pending＝確認済み／shipped＝配達中／delivered＝配達済み）。取り込みのたびに注文一覧から既取込の注文も更新（詳細は開かない）。在庫は確定時に作り、到着まで仕入に「未発送／配送中」、在庫に「未着」を出す。紐付けは到着前でも可（ユーザー決定）。`inventory_view` に `fulfillment`
- **画面**：ページ幅を画面に追従（設定だけ 1100px）。売上・在庫・仕入の行を「商品名／状態チップ／メモ」の 3 段に組み直し（「自動取得」は販売日の下から状態チップの列へ、メモは「メモ」ラベル＋アイコン付きで本文と区別）。仕入に「自動取得」チップと代表商品名（`first_line_name` / `first_model_code`、「ほか n 点」）
- **サムネイル**（schema_version 6）：メルカリの販売履歴の画像を、新規の販売と未保存の既知の販売について 1 回の収集で最大 30 枚・300〜800ms 間隔で `userData/thumbs/<itemId>.jpg` に保存（`sale.thumb_file`）。独自スキーム `soroban-thumb://` で画面に出す（CSP に `img-src soroban-thumb:`）。売上・在庫の行頭に 40px、無ければ型番の頭文字のプレースホルダ。メロジョイ側は注文画面に画像が無い（後日、ストアの商品検索 JSON で引く案）
- **見た目を「Milestone」風の温かい帳簿へ**（ユーザーが参考画像で指名）：黄色のブランド色・白い角丸カード（罫線なし）・ベタ塗りのピル型チップ・黄色の主役カードに大きい数字（ホーム）・チャコールの主ボタン・88px の縦積みナビ・丸アイコン付きの見出し・月次の合計行。意味色（利益＝緑／損失＝赤／要対応＝橙）は維持。DESIGN.md / design.json を実装から書き直し
- 表の操作列（タグ／メモ／削除）と設定の仕入先の名前セルが `display:flex` で行から外れて描かれていたのを修正
- **OS の `confirm()` / `alert()` を全廃**（ユーザー指示「ダサい」＋ Electron でネイティブダイアログの直後にキー入力が効かなくなり、仕入先を続けて追加できなかった）。`components/ConfirmDialog.vue` と `App.vue` の `provide('confirm' | 'choose' | 'toast')` に置き換え。削除系は赤い主ボタン、成功はトースト、検証エラーは warn トースト。仕入先の追加は「名前 → 種別を 3 つのボタンで選ぶ」
- **フォントを M PLUS 2（可変ウェイト）に**（ユーザー指示）。`@fontsource-variable/m-plus-2` を同梱し `main.ts` で import（CSP は `'self'` のまま）
- 仕入先の削除時は実行記録の `shop_account_id` を外して残す
- テスト 128 件。実 DB のコピーで v3→v6 のマイグレーションを確認

## 2026-09-20 Phase 3：在庫 1 点の履歴と商品（型番）ページ

- **履歴ドロワー**（`components/TimelineDrawer.vue`、`getItemTimeline`）：注文（仕入先・注文番号・単価＋送料按分→原価）→ 仕入先が発送 → 到着 → メルカリで売れた（価格・買い手）→ 発送 → 受取 → 取引完了（売上金の反映）を時系列で。日付が無い段は「予定」。在庫・売上の行のサムネ／商品名、商品ページの在庫行から開く
- **商品タブ**（`views/Products.vue`、`listProducts` / `getProduct`）：型番ごとに 在庫数・仕入合計・平均原価・平均売価・平均粗利・粗利合計・最終仕入／販売日。詳細で月ごとの仕入数／販売数／在庫残を依存なしの SVG（`components/MiniChart.vue`）で棒＋折れ線。ホームのランキング行から飛べる
- **schema_version 7**：`purchase.shipped_at` / `delivered_at`（到着状態が変わったのを最初に観測した日。既存分は `fulfillment_updated_at` で埋める）、`sale.status` / `shipped_at` / `delivered_at` / `completed_at` / `buyer`（メルカリの取引中タブ・取引画面の DOM 待ち。まだ null）
- テスト 136 件。実 DB のコピーで v6→v7 を確認

## 2026-09-20 出品タブ・仕入タグ・全画面検索（要件 3-C）

- **出品**（schema 8）：`listing` / `listing_line`。収集のたびに出品中タブを 1 ページ読み、キーワード一致だけ upsert。出品は在庫の状態を変えず、引き当ては `listing_line` が持つ（二重引き当て・未販売以外はトリガーで拒否）。売れた販売の id と同じ出品に引き当てがあれば `sale_line`（`link_source='listing'`）へ移す。1 ページから消えただけでは解除しない
- **出品タブ**（`views/Listings.vue`、`components/AllocateDrawer.vue`）：状態・未引き当ての絞り込み、引き当てドロワー（チェックした瞬間に見込み粗利）、取り下げ。ホームの要対応に「未引き当ての出品」
- **仕入タグ・派生タグ**（schema 9）：`purchase_tag`。仕入→在庫→販売へ派生（`inherited_tags`、コピーしない）。画面では薄いチップ。タグ絞り込みは 直接 ∪ 派生
- **検索**（`components/SearchBox.vue`）：売上・在庫・仕入・商品・出品。空白 AND・NFKC。検索中は状態の絞り込みを外す
- **在庫**：未出品／出品中／販売済／その他。出品中の行に価格チップ。商品ページに「未出品 n・出品中 m」
- **守りの修正**：`linkInventory` が販売済み・廃棄済みを拒否。廃棄・分割で引き当てを外す。シリーズだけの型番（`A035`）は自動確定せず候補止まり（CLAUDE.md に明文化）
- **ペルソナ E2E**：`src/main/__tests__/scenarios/persona-01…20`（20 人の使い方を API に流して手計算で検算）。テスト 189 件
- Codex に画面構成をレビューしてもらい、「検索中は絞り込みを外す」「引き当て済みの行からも編集」「廃棄時の引き当て解除」を反映。横断検索・返品は保留

## 2026-09-20 深夜（自走）期間費用・純利益・按分の整数統一・出品タブの仕上げ

- **期間費用（R-05、schema 10）**：`expense.auto`、`expense_auto_month`。販売用の販売がある月に振込手数料（設定値）を 1 件だけ自動計上（作った時点の値で固定。人が消せば再作成しない。販売用が 0 件になった月からは自動行を消す）。月次に「期間費用」「純利益」列と内訳の展開（追加・削除）。ホームに「純利益」
- **送料未入力の注記**：月次の行に「送料未入力 n 件」チップと注記（送料 0 で集計されている）
- **まとめ売りの按分を整数で統一**：ビュー `sale_line_share`（1 販売の価格・粗利を点数で整数按分、端数は最後の行）。`variant_summary` / 商品ページ / 履歴ドロワーの 1 点あたりが全部これを使う。合計が販売と必ず一致（persona-02 の 1 円ずれ解消）
- **守りの修正**：引き当て中の在庫を持つ仕入の削除が FK で失敗していた → 引き当てを外してから消す。未引き当ての出品が売れたとき出品が `active` のまま残っていた → `sold` にする
- **出品タブの仕上げ**（デザインレビュー 8 件）：狭幅の折り返し・列詰め、「見込み粗利（送料前）」の明示、未引き当て行の「引き当て」を紐付けと同じ warn 枠に、「出品中」チップを info に、解除をテキストに、ヒント 13px、粗利セルの settle 遷移
- 要件 7.3（人の入力と自動取得の優先順位）を追記。テスト 194 件。実 DB のコピーで v7→v9 の移行を確認（v10 も同じ経路）

## 2026-09-20 早朝（自走）横断検索・引き当ての移動・出品の最終確認

- **横断検索**（`components/GlobalSearch.vue`、`searchAll`）：ヘッダの検索欄（Ctrl+K）で在庫／出品／売上／仕入を一度に探す。空白 AND・NFKC。結果は右ドロワーに種類ごと（状態チップ・金額・日付）。クリックで該当タブへ飛び、検索語を引き継いで該当行をハイライト。Codex 提案「あの商品どうなった？を 1 か所で」
- **引き当ての移動**：別の出品に引き当て済みの在庫も候補に出し（「出品 ¥… に引き当て済み」チップ）、チェックして「引き当てる（n 点を移す）」で付け替え。再出品・付け替えの導線（`reserveInventory` が元の引き当てを外して移す）
- **出品タブ**：「最終確認 MM/DD HH:mm」を併記。直近のメルカリ取り込みで見えなかった出品に「前回の取り込みで見えず」チップ（1 ページ目に無かっただけかもしれないので引き当ては維持）
- テスト 207 件
- **要対応の先頭に取り込みの失敗**（`DashboardStats.recentRuns`：取り込み元ごとの直近 1 件）。メルカリ／メロジョイ口座ごとに、ログインが必要・失敗・0 件 を要対応の一番上に出し、「ログインして取り込む」「もう一度取り込む」で即再実行。最後の 1 回だけ見て失敗が隠れる問題を解消（Codex 指摘 5）。ペルソナ E2E 21〜24 追加。テスト 213 件

## 2026-09-20 昼（自走）Codex コードレビューの反映（schema 11）

Codex に main 側（出品・派生タグ・期間費用・整数按分・横断検索・collector）をレビューさせ、確度の高いバグ 7 件と仕様の注意 1 件を修正。
- **P1** ホームの純利益が常に 0（`getDashboard` が `monthly_summary` を直読みしていた）→ `listMonthly` 経由に
- **P1** 終了済み（sold／ended）の出品に引き当てられ、在庫が二重に見える → トリガーと `reserveInventory` で拒否（v11 でトリガー作り直し）。画面も終了済みの行から操作ボタンを消し、ドロワーに「この出品は終了しています」
- 出品から引き継いだ販売は、キーワード不一致でも `kind='resale'`（人が引き当てた＝販売用の意思）
- `listing.last_seen_at` を ISO に統一（v11 で既存行を変換）。「前回の取り込みで見えず」の判定を Date 比較に（文字列比較で常に真だった）
- 出品一覧・引き当て候補の検索を他画面と同じ NFKC・AND に（横断検索から飛ぶと一覧で消えていた）
- collector：販売 0 件でも出品中タブを読む／総数は読めるのに 0 件解析なら「構造が変わった可能性」を message に／サムネ予算（30 枚／回）は成功数ではなく試行数で減らす
- 按分の規則の表現：均等割り（分割・まとめ売り）は「余りを末尾の行に 1 円ずつ」。仕入の按分（端数は最終行）と区別して CLAUDE.md・要件に明記
- テスト 222 件。実 DB のコピーで v7→v11 を確認

## 2026-09-20 昼（2）売上＋出品の統合・サマリとグラフ・仕入先の取り込みキーワード・メルカリの除外

- **売上タブに出品を統合**（出品タブは廃止）：段階の切替 出品中 (n)｜未処理 (n)｜完了｜すべて（既定は未処理）。1 行＝メルカリの 1 商品で、出品中→売れた→完了と同じ行の形で流れる。ドロワーは 1 つ（`AllocateDrawer` の mode: listing／sale）。「すべて」は閲覧用に列を絞る
- **サマリとグラフ**（`components/SalesSummary.vue`）：今月の粗利（売上・件数・純利益）／出品中 n 件と見込み粗利／未処理 n 件。直近 12 か月の 売上（棒）・粗利・純利益（線）を 150px の SVG で。販売用／私物の切替、隠す／見せる（記憶）
- **仕入先ごとの取り込みキーワード**（schema 12、`shop_account.import_keywords`）：商品名が一致する明細だけ取り込む。一致 0 件の注文は取り込まない。一部一致は一致した明細だけを仕入にし、送料・割引は注文全体の金額比で按分（例：2 明細 2,499＋2,399、送料 499 → 一致 1 明細に 255）。下書きは按分できないので注文全体の値のままメモに残す。設定の仕入先の行で編集
- **メルカリもキーワード不一致は取り込まない**（販売も。詳細・サムネも取りに行かない。除外数を run の message に）。それまでは不一致を私物として取り込んでいた
- **TikTok Shop の自動取り込みは取り下げ**（購入履歴が PC から見られない）。手動登録のまま
- テスト 232 件

## 2026-09-20 午後 出品日の推定・いいね・出品時の発送方法・型番で自動引き当て・区分名（schema 13）

- **出品日**：出品中タブの「n日前に更新」から推定（今日 − n 日）。以後の取り込みでは古い方だけ残す（値下げ等で更新が巻き戻っても消えない）。「初めて見た日」をやめ、画面は「出品日（更新日から推定）」
- **いいね数**を出品中の行にチップで
- **出品時の発送方法**：出品中の行で発送方法を決めておくと、売れたとき販売にそのまま入る（送料・確定・manual。取引画面の実額が取れたらそちらが優先）。見込み粗利は決めた送料を引く
- **型番で自動引き当て**：出品中にもボタン。枝番まで完全一致・先入先出 1 点・1 クリックで解除
- 区分名：出品中｜**売れた・要入力**｜**利益確定**｜すべて。段階の下に 1 行ヒント
- テスト 235 件
- **自動確定の規則を緩和**：「枝番あり」の条件をやめ、**抽出した型番が 1 つで在庫の `model_code` と文字列が完全一致**なら自動（`A037` のようにバリアントの無い商品も自動で引き当たる。在庫が `A035-1` で出品が `【A035】` なら候補止まり）。出品中の「確認 MM/DD HH:mm」の表記を短くしてサムネに被らないように

## 2026-09-21 仕入の取引詳細・自動タグ・商品タグ・送料無料（schema 14）

- **メロジョイ：送料「無料」を 0 円に**（#270882 が「配送料が読めません」で下書きになっていた。半角 ¥ にも対応）
- **仕入の取引詳細ドロワー**（`components/PurchaseDrawer.vue`）：行のタイトル・サムネ・注文番号をクリック。明細（商品名・型番・数量・単価・1 点原価・小計）と、その明細から生まれた在庫 1 点ずつの状態（未出品／出品中 ¥／販売済 ¥ 日付 → 売上の行へ）、商品計・送料・割引・総原価・按分方法、メモ。下書きは「確定する」
- **仕入先の自動タグ**（`shop_account_tag`）：設定の仕入先で決めたタグが、その仕入先の仕入に登録時に自動で付く（後から外せる）。在庫・販売まで派生。用途例：マージン対象の口座
- **商品（型番）タグ**（`product_tag`）：商品タブで付ける。その型番の在庫・販売に派生
- **タグの出どころ**：派生タグのチップに 仕／品／在 の印と title。絞り込みの候補を 直接／仕入から／商品から／在庫から で分類。派生の優先は 仕入 > 商品 > 在庫（`Tag.from`）
- テスト 245 件

## 2026-09-21 自動アップデート（GitHub Releases）

- `pnpm release:patch`（バージョンを上げてタグを push）→ GitHub Actions（`.github/workflows/release.yml`）が mac（dmg arm64/x64）と Windows（NSIS）をビルドして Releases に publish
- アプリ（`src/main/updater.ts`、electron-updater）は起動 10 秒後と 6 時間ごとに確認。Windows は自動ダウンロード → 終了時に入れ替え。macOS は署名が無いので Releases ページを開く（dmg を上書き。DB はそのまま）
- 設定に「アプリの更新」パネル（現在のバージョン・更新を確認・更新する）。ヘッダ下にお知らせバナー
- テスト 255 件
- **リリースの安定化**：macOS 27 で Electron 32 が起動直後にクラッシュ → **Electron 42**（better-sqlite3 12.11.1 のプリビルドがある最新）。GitHub Actions は mac を macos-14 ランナー＋Spotlight 停止（`hdiutil detach` の Resource busy）、mac/Windows の artifact を集めて最後に 1 つの Release を公開（electron-builder の publish は Release が 2 つできるのでやめた）。**v0.1.6 が最初のまともな公開版**。README にインストール・クリーンインストール・自動アップデートの節
- **macOS で起動直後にクラッシュ（v0.1.0〜v0.1.8）の真因**：electron-builder が `productName`「そろばん」からヘルパーアプリ名（`そろばん Helper.app`）と `CFBundleName` を日本語にしていて、Electron の起動直後のヘルパー探索（`electron_main_delegate_mac.mm`）で `EXC_BREAKPOINT`。Electron のバージョン・署名・entitlements・実行ファイル名は無関係だった（素の Electron.app に app.asar を入れると動く、で二分探索）。**v0.1.9** で内部名を `Soroban`、表示名だけ「そろばん」（`mac.extendInfo.CFBundleDisplayName`、`nsis.shortcutName`）にして解消。macOS 27 では Gatekeeper の「右クリック → 開く」が通らず `xattr -dr com.apple.quarantine` が要る

## 2026-09-21 在庫コード・取引中タブ・取引状態・公開準備（schema 16）

- **在庫コード**（`inventory_item.item_code`、`S-0001` から採番・UNIQUE）：在庫 1 点ごとの固有コード。分割の子・セット売りなど、型番の先入先出では選べないときだけ使う。在庫・仕入ドロワー・売上の原価セルにチップ（`components/CodeChip.vue`）、タップで `【S-0012】` の形でコピー。**日常の引き当ては従来どおり型番の完全一致・先入先出**（`【A037】×2` / `2個` で 2 点）。タイトルに在庫コードがあればそれを名指しで引き当て（`findInventoryIdsByItemCodes`）。既存の在庫には移行時に古い順で振る
- **取引中タブの取り込み**（`parseInProgressHtml`、fixture `mercari-in-progress.html`）：販売履歴に出る前の「支払いをしてください／発送してください／受取評価待ち／評価をしてください」を 1 ページだけ読み、未知なら販売として登録（販売日は初めて見た日）、既知なら状態を進める（`updateSaleStatus`、後戻りしない。`shipped_at` / `delivered_at` は初到達日）。販売履歴に出たら `completed`。キーワード不一致は取り込まない。サムネの予算（30 枚/回）は出品中・取引中で共有
- **取引状態**：売上の行にチップ（発送してください＝琥珀）、「すべて」に状態列、状態で絞り込み。ホームの要対応に「発送してください n 件」（`DashboardStats.needsShipment`）
- **メルカリで開く**（`openMercari`）：売上の行から商品ページ／取引画面を既定のブラウザで開く。`m` + 数字の ID だけ許可
- `sale.status` の CHECK に `waiting_payment` を追加（schema 16、テーブル作り直し・データはそのまま）。実 DB のコピーで 14→16 を確認（販売 18・在庫 4・粗利が移行前後で一致）
- **公開準備**：README / PRODUCT / CLAUDE / DESIGN / docs から「転売」の表現を除去（画面の区分名「転売／私物」は据え置き）。fixture の私物の例を一般名に。秘密情報・個人情報が無いことを 95 ファイルで確認（メロジョイの fixture は `example@example.com` / `〒000-0000` にマスク済み）
- テスト 289 件
- **ヘルプ画面**（`views/Help.vue`、内容は `help/topics.ts`）：ナビの末尾。「こういうときは…？」形式で 9 カテゴリ 32 トピック（分割・セット売り・在庫の名指し引き当て・タイトルの書き方・自動で引き当たらない理由・取り込み・タグ・費用・バックアップ／更新）。検索とカテゴリ目次、各トピックから該当タブへ飛ぶボタン

## 2026-09-21 夕 v0.1.11 — 画面からの登録が失敗していた回帰・送料 ¥0・絞り込みの統一・グラフ・税込

- **重大：Electron 42 に上げてから（v0.1.6〜v0.1.10）「登録して在庫を作る」「販売を登録」など、フォームのオブジェクトをそのまま IPC に渡す操作が全部黙って失敗していた。** contextBridge の境界に Vue のリアクティブ（Proxy）を渡すと同期例外 `An object could not be cloned`（Electron 32 では通っていた）。`window.soroban` は差し替え不可なので、preload は `sorobanBridge` として公開し、`renderer/main.ts` が全メソッドの引数を JSON で素の値に直した包みを `window.soroban` に定義。`app.config.errorHandler` → 画面の通知に出す（黙って失敗させない）。**実アプリを CDP（`--remote-debugging-port`）で動かして再現・修正確認**（モックとテストでは再現しない。`SOROBAN_DB_PATH` / `SOROBAN_NO_COLLECT` を検証用に追加）
- **送料 ¥0 の扱い**：販売履歴の送料はメルカリ便のときだけ入り、自分で郵便などを使うと ¥0。従来は「実額 ¥0」で確定・固定していた（先に発送方法を選んでも次の取り込みで上書きされ、直せない）。新しい規則：¥0 は**送料未入力**（要対応に出る）、人が選んだ発送方法（`master`）と出品時に決めて引き継いだもの（`manual`）は ¥0 で上書きしない、メルカリ便の実額（>0）は従来どおり優先。`takeOverListing` も「実額で確定」のときだけ引き継ぎを止める。画面は実額 ¥0 のときセレクトを出す（「選択…（メルカリ便以外）」）
- **売上タブの絞り込みを全段階で同じ並びに**：状態｜タグ｜検索。出品中の状態は すべて／未引き当て／引き当て済み（チェックボックスを廃止）。出品中はタグで絞れないので disabled＋理由を title に
- **グラフ**：ホバーで月・売上・粗利・件数・純利益のツールチップ（縦の帯で当たり判定）、クリックでその月の販売を売上タブ「すべて」に月チップ付きで表示（段階を切り替えると解除）
- **在庫タブの状態セレクトに「すべて」**（状態順に並べ、状態チップを出す）
- **税込の明示**：金額はすべて税込（実際に払った・受け取った額）。仕入フォーム・販売フォーム・設定・月次の入力欄に「（税込）」、ヘルプに「税込で入れる？税抜で入れる？」
- テスト 290 件

## 2026-09-21 夜 v0.1.12 — 配送状態の手入力・期間と並び替え・送料の既定値・CSV 一括登録（schema 17）

- **仕入の配送状態を手で変える**（`updatePurchaseFulfillment`）：TikTok Shop など自動取得しない仕入先向け。登録フォームの「配送状態」、一覧の行とドロワーの「配送」ボタン（2 クリック）。shipped / delivered に初めて到達した日を刻み、後戻りしても日付は消さない
- **期間の絞り込みと列の並び替え**：共通部品 `PeriodSelect`（すべて／今月／先月／直近 3 か月／今年／期間を指定）・`useSort`・`SortTh`。仕入（注文日。下書きは常に先頭）・在庫（仕入日）・売上（販売日。月チップと AND）・商品（最終販売日）。toolbar は折り返し可、日付・件数は折り返さない
- **仕入先ごとの送料の既定値**（`shop_account.default_shipping_fee`、schema 17）：設定 → 仕入先で入力。登録フォームで仕入先を選ぶと入る（人が触った値は上書きしない）。CSV でも送料が空ならこれ
- **注文番号の重複を日本語で断る**：「この仕入先には注文番号「X」の仕入が既にあります（日付 の登録）」。数量 0・日付の形式なども。通知の `Error invoking remote method` の接頭辞は除去。削除した仕入は物理削除なので制約に残らない
- **CSV で一括登録**（`components/PurchaseCsvDrawer.vue`、`utils/csv.ts`、`importPurchases`）：ひな形（UTF-8 BOM）→ 読み込み（UTF-8／Shift_JIS）→ プレビュー（問題は理由付き）→ 登録。1 行＝商品 1 つ、同じ注文番号の行は 1 つの仕入。重複は同じ仕入先の同じ注文番号だけ（空は見ない）。失敗した行は理由を返し、他は登録する
- テスト 298 件。実アプリ（CDP）で重複エラー文を確認

## 2026-09-21 夜（2）v0.1.13 — 経費タブ・月の明細と按分・締め（schema 18）

- **振込手数料の自動計上を撤去**（`ensureTransferFees`・`expense_auto_month`・設定の項目）。過去に入った行は普通の経費として残る
- **経費タブ**（`views/Expenses.vue`、`components/ExpenseDrawer.vue`）：レシート 1 枚＝経費 1 件。購入店・購入日・明細（品名・金額・数量・項目。既定は梱包費）・計上月（既定は購入月）・メモ。ドロワーでレシート画像を添付（`main/receipts.ts`、`userData/thumbs/receipt-<id>.<ext>`、`soroban-thumb://` で表示）。項目：梱包費／消耗品／送料／手数料／振込手数料（旧）／その他。OCR は無し
- **月の明細**（月次タブの行をクリック、`views/MonthDetail.vue`、`getMonthDetail`）：片付けるもの（送料未入力・未紐付け）→ 売上タブへ、合計カード（売上・粗利・経費・純利益）、**経費の按分**（その月の販売用の販売に、金額＝販売価格の比／数量＝点数の比。floor で配って余りは最後の行。私物には配賦しない）、販売の表（按分経費・按分後利益・合計行）、**タグで絞ると「そのタグの分：粗利−按分経費」**（マージン対象の口座に払う額）、経費の表（項目別小計）、仕入先への支払い（その月の確定仕入の総原価）、私物は折りたたみ
- **締め**（`month_book`）：終わった月だけ「この月を締める」。締めた時点の数字を記録し「締め済み」。編集はロックせず、後で数字が変われば「締めた後に変わった」と差分。解除可
- 数字の確認：ペルソナ 26（経費 1,001 円・販売 1000/2000/3000・点数 1/1/2 → 金額按分 166/333/502、数量按分 250/250/501）。実 DB コピー（14→18）で 9 件に 2,200 円を配って合計一致（金額・数量とも）。今月は締められない・先月は締められることを実アプリで確認
- 撤去：ペルソナ 24（振込手数料の自動計上のテスト）。テスト 300 件

## 2026-09-21 深夜 v0.1.14 — 経費の単価×数量・レシート読み取り（暫定）・平均のまるごと換算・検索の AND・操作ログ・店名の学習（schema 20）

- **経費の明細を「単価 × 数量 ＝ 金額」に**（schema 19。既存行は `amount / quantity` で単価を逆算）。ユーザー報告「数量 4 と入れても合計されない」
- **レシート読み取り（暫定）**：経費フォームの「レシートを読み取る」→ 画像を選ぶ → 店名・日付・明細を推定してフォームに（Tesseract 同梱、オフライン）。登録時にその画像をレシートとして添付。合計と明細が合わなければ警告。**本物の写真では精度が足りない**（信頼度 20 前後）ため、次の版で Gemini（利用者の API キー）に置き換える。Windows 標準 OCR／mac Vision の検証コード（`resources/win/soroban-ocr.ps1`、実レシートの単語データ）は参考として残し、mac の Swift ヘルパーと CI のビルドは取り下げ
- **購入店の候補**：過去の登録から使用回数順の datalist。**店名の学習**（`shop_alias`、schema 20）：レシートの事業者登録番号（T＋13 桁）→ 店名 を登録時に覚え、次の読み取りで使う
- **商品タブの平均売価・平均粗利を「まるごと換算」**：分割した子は 1/分割数 の重み（孫は再帰）。まるごと ¥3,600 ×1 と 4 分割 ¥1,000 ×4 → 平均売価 ¥3,800／箱、平均粗利 ¥420／箱。件数は「点」のまま
- **検索と他の絞り込みを AND に**（在庫・売上）。「検索中は状態の絞り込みを解除」をやめた。Ctrl+K から飛んだときだけ状態を「すべて」に
- **操作ログ**（`app_log`、`main/applog.ts`）：IPC の登録・更新・削除・紐付け・取り込み・締め、収集の結果、画面の例外を DB に記録（読み取り系は記録しない）。14 日より古い行は起動時に削除。不具合の報告は「データベースをバックアップ」のファイル 1 本で済む。認証情報はもともと扱わないが `password`／`token`／`cookie` は伏せる
- テスト 325 件

## 2026-09-21 深夜（2）v0.1.15 — 型番の表示名・仕入先の絞り込みと累計・分割を戻す（schema 21）

- **型番の表示名**（`product_name`）：商品タブの「名前」で付ける／空で解除。無ければ従来どおり「その型番で最後に仕入れた明細の名前」。仕入明細・在庫の元の名前は変えない。商品タブ・ランキングに反映
- **仕入先ごとの明細**：仕入タブに仕入先の絞り込み（絞ると 件数・明細数・商品計・送料・支払合計）。設定の仕入先一覧に累計（注文・点数・支払・最終注文日、`listShopAccountStats`）
- **型番ランキングの商品名**：2 行まで折り返し＋ホバーで全文
- **分割を戻す**（`mergeSplitInventory`）：子が全部未出品で出品に引き当てていないときだけ。子を消して親を未出品に戻す（原価は分割前のまま＝子の合計と一致を確認）。子のタグ・メモは親に寄せる。戻せない理由は `S-0023 が販売済みです` のように全部まとめて出す。在庫タブの子の行「分割を戻す」／親の行「戻す」
- テスト 334 件

## 2026-09-22 v0.1.16 — Codex レビューの反映（金額・按分・紐付け・取り込み）・導線（schema 22）

Codex に全体レビューを依頼（`docs/02-use-cases.md` の観点も含む）。13 件の指摘のうち 12 件を修正、1 件（自動紐付けの `×2`・在庫コード名指し）は仕様なので CLAUDE.md を実態に合わせた。
- **仕入 0 円で送料が原価から消える**：金額按分で重みの合計が 0 なら数量按分に切り替え（`allocate`）。実 DB コピーで 単価 0・送料 500 → 原価 500 を確認
- **金額の整数チェック**（`assertYen`／`assertQty`）：仕入・販売・発送方法・経費・仕入先の入力で小数・負を日本語で断る（`1行目の単価は整数で入力してください`）
- **送料の後発の実額を無視**：再取り込みのスキップ判定を値の比較に（¥0 → ¥210 で更新される。`master`／`manual` は守る）
- **価格 0 の月の按分**が合わない → 数量按分に切り替え
- **複数項目のレシート**が先頭項目に全額 → 明細の項目で集計
- **在庫コード名指しが別の出品の予約を奪う** → 候補止まり
- **在庫コードの採番**：開始番号は max(カウンタ, 既存の最大)、中断しても再開できる
- **削除した販売の再取り込み防止**（`sale_exclusion`）：設定 → データ に一覧と「戻す」
- **粗利の見積もりを main に**（`estimateSaleProfit`）：紐付けドロワーが再計算しない。削除済み発送方法の送料も引く
- **取り込み**：キーワード不一致の既知販売はサムネ・詳細を取りに行かない／起動時と手動の収集は同時に 1 つ／メロジョイの不一致注文は除外記録（キーワードが変われば再評価）で枠を占有しない／詳細が全件失敗なら `failed`／取引中タブの件数を `fetched`・`inserted` に含める
- **導線**：ホームの「発送してください」は状態で開く（送料入力済みも見える）／「長期滞留」→ 在庫（すべて・滞留 n 日以上）／「ログインする」はログイン画面を開く／月の明細でタグ絞り込み中に別タグも付いた販売があれば注意（重複計上）／売上タブ内の変更で上のサマリが即時更新
- **ユースケースの整理** `docs/02-use-cases.md`（◎○✗）。画面構造の見直し（ホーム＝受信箱、売上＝進捗＋作業リスト、仕入＝伝票ヘッダ、在庫＝型番ごと、商品＝カルテ、月次＝計算書、経費＝2 ペイン）はモックで合意、次の版から
- テスト 369 件

## 2026-09-22 v0.2.0 — 画面の作り直し・AI レシート読み取り（Gemini）・バックアップ zip

Codex の「画面構造の参考例」と利用者との合意（`docs/mocks/*.html`）に基づく大型アップデート。数字は全部 main（`inbox.ts`／`views.ts`）が計算し、画面は表示と操作だけ。
- **ホーム＝受信箱**：利益ストリップ（今月の粗利／確定待ちの粗利（見込み）／売上金の反映待ち／先月の純利益）、「今やること」（発送する → 送料を入れる → 在庫を紐付ける → 仕入の価格を入れる → 取り込みの問題 → 忘れていませんか）を行内で片付ける（発送方法セレクト・候補 1 点の紐付け・確定・ログイン・スヌーズ 7 日）、行ごとに「片付けると粗利いくら」、右に「見直すもの」。トップバーに今月の粗利／純利益を全タブ常設
- **売上＝進捗ストリップ＋利益の入力＋作業リスト**：出品中 › 売れた・発送する › 配送中・受取待ち（売上金の反映待ち） › 取引完了 › すべて。入力の軸（送料未入力／未紐付け／入力済み）は別。未確定の行が上、粗利の欄に確定後の幅
- **仕入**：上に仕入先カード（期間内の支払・注文・点数・下書き・到着待ち・ログイン要）、行はヘッダ情報＋配送の進行（注文 › 発送 › 配送 › 到着）、検索は明細の商品名まで（表示中 50 件を並列取得・キャッシュ）。表＋ドロワーは維持
- **在庫**：状態カード（届いていて未出品／未着／出品中／長期滞留）、型番ごとのまとまり（画像・実績・1 点ずつ）と「1 点ずつ」の切替、**在庫 1 点の追跡ドロワー**（お金 4 枚・足あと・いっしょに買ったもの）
- **商品カルテ**：左に型番一覧、右に情報・数字 5 枚・**この金額で売ったら？**（`estimateSaleProfit`）・手元の在庫・販売の履歴
- **月次＝計算書**：月の帯、売上 → 手数料 → 送料 → 梱包 → 原価 ＝ 粗利 → 経費 ＝ 純利益（率付き）、参考（売上金の反映待ち・私物・仕入の支払）、販売ごとの表はそのまま、右に 締め／タグ別の集計（複数タグの注意）／仕入先への支払い
- **経費＝レシート×フォームの 2 ペイン**：「レシートを読み取って登録」→ Gemini（利用者の API キー。設定 → AI 読み取り。safeStorage で暗号化、`getSettings`・IPC ログから除外）で店名・登録番号・日付・明細＋項目・合計・税を JSON で。**読んだ位置を半透明の枠で画像に重ね、欄のホバーで光る**。税の差額を 1 クリックで追加。登録番号 → 店名の学習。Tesseract と同梱モデルは撤去（−43MB）
- **バックアップ zip**（DB を backup API で整合＋レシート画像・サムネ＋meta）と **復元**（退避してから入れ替え・再起動。zip slip 対策）。不具合報告はこの 1 本
- 共通：ナビ順（経費を月次の前、設定・ヘルプは区切り線の下）、Milestone に合わせたトークン（黄 #fbcf52・塗りピル・グラフのパレット）、`StatusPill`／`StageStrip`／`ProfitStripBar`、期間に「直近 6 か月」「昨年」
- Codex の最終レビューは途中で止まった（クレジット切れ）が、出ていた 2 件（API キーが IPC ログに平文で載る／復元の zip slip）を修正。実 DB コピーで全 API・全タブ遷移を例外ゼロで確認。テスト 395 件
