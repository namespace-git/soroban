# 2026-09

出荷ずみ（統合ずみ）の実装記録。1件あたり数行で「何を・なぜ・どう確かめたか」。

## 2026-09-19 第1波 — Phase 1 の穴埋めと金額バグ修正

- **按分の1円消失を修正**（`src/main/money.ts` `splitEvenly`）。明細内のアイテムに配賦額を割るとき、余りを最後の数点に +1 して Σlanded_cost が「明細合計＋配賦額」と必ず一致するようにした。`listPurchases.total_cost` も同じ式に。テストで 100円/3点 → 1033/1033/1034 を確認
- **日付を JST に統一**（`src/shared/date.ts`）。ダッシュボードの月キー・収集の販売日・仕入フォームの初期値が UTC でずれていた
- **候補提示の正規化**（`suggestInventory`）を JS 側に寄せ、販売タイトルと在庫名に同じ正規化を掛けるようにした
- **販売の手入力**（S-02）、**紐付けの解除・付け替え**（M-05, `listSaleLines` 追加）、**梱包材費**（S-07）、**自家消費**（I-05, `disposeInventory` に status）を追加
- 紐付けパネルの粗利プレビューが `packaging_cost` を引いていなかったのを修正
- **テスト基盤**: vitest 3（vite 5 対応）。`better-sqlite3` が Electron ABI なので `pnpm test` は `ELECTRON_RUN_AS_NODE=1 electron vitest` で回す。16件
- 環境: pnpm 10 の build script 許可（`pnpm.onlyBuiltDependencies`）、`tsconfig` に `vite/client`、`@types/better-sqlite3`

確認: `pnpm typecheck` / `pnpm build` / `pnpm test` すべて緑。**画面は実機未確認**（エージェントは Electron を起動しない方針）

## 2026-09-19 第2波 — 画面の再設計（業務SaaS標準型）

- **見た目を全面的に置き換えた。** 方針は「業務SaaSの標準型」、品質基準はマネーフォワード クラウド（`PRODUCT.md` Brand Commitments、`.impeccable/surfaces/src-renderer-app-vue.md` に契約）。ライト。寒色グレー地＋白面＋1px罫、ブランド青は主ボタン・選択・フォーカスだけ、利益=緑／損失=赤／要対応=琥珀は数字とチップに限定。トークンと部品の記録は `DESIGN.md` / `.impeccable/design.json`
- **構成の変更**: 左サイドナビ（220px、1100px 未満でアイコン帯）＋上部バー。月次を在庫のサブタブから独立ページ `Monthly.vue` へ。紐付けはモーダルをやめ右ドロワー（`components/Drawer.vue`、背後にテーブルが見える）。売上ナビに未処理件数バッジ（`listSales({onlyPending})` の件数。子 view は `inject('changed')` で更新を通知）
- **共通部品**: `components/{Icon,Drawer,StatusChip,EmptyState,Skeleton}.vue`。アイコンは自前 SVG 20種（絵文字・記号を廃止）。`style.css` は全面置換（クラス語彙は `DESIGN.md`）
- **売上タブ**: 行 49px（チップは商品名の右）、発送方法セレクトのラベルを「名前　¥送料」に、未確定の select は琥珀地、発送方法を選ぶと粗利セルが 180ms で入れ替わる。ドロワーの候補行は 32px ピッチ（30〜100点向け）。操作数は変えていない
- **負値の表記**を `−¥1,234` に統一（Dashboard / Sales）
- **開発用モック** `src/renderer/mock/soroban-mock.ts`: `import.meta.env.DEV && !window.soroban` のときだけ動的 import。素のブラウザで全画面を動かせる（`.claude/launch.json` の `renderer` = `electron-vite dev --rendererOnly`）。本番バンドルには入らないことを `pnpm build` の出力で確認
- **レビュー**: impeccable の detector `[]`、finish-reviewer 2ラウンド（8件＋3件を修正）。最終ラウンド後に残った2件（設定の送料セルの `¥` の間隔、1366px での発送方法ラベルの省略）は親が幅とラベル形式を直し、スクリーンショットで確認。**reviewer の再採点は受けていない**
- **ロジックは変えていない**: `src/main/` `src/preload/` `src/shared/` は無変更。文言も現状踏襲（新規は「実行履歴」「全件を表示するには…」の2つ）

確認: `pnpm typecheck` / `pnpm build` 緑。画面は隠し Electron ウィンドウで 1366×768 と 1024×700 を撮影して確認（`.impeccable/review/`、git 対象外）。**実機（`pnpm dev`）での操作確認は未実施**

## 2026-09-19 Phase 2 — 型番で紐付ける

- **契約・DB**（`shared/types.ts` `main/schema.sql` `main/db.ts` `main/code.ts`）：`model_code` / `series_code` / `material` を仕入明細と在庫に。`【Z078-2】` の抽出は `code.ts`（バリアント名に【】があればそれ、なければ商品名側。`Z048-10` を「Z048 の 10 番」と解釈しない）。マイグレーションは `schema_version` で管理し、`inventory_item` は CHECK を変えるためテーブル再構築
- **自動紐付け**：枝番まで完全一致した転売だけ、`acquired_at` の古い順に1点を `link_source='auto'` で確定（「自動確定しない」の唯一の例外）。シリーズ一致・複数型番・型番なしは候補提示のまま。`autoLinkPending()` で溜まった分を一括
- **分割**（ばらし売り）：`splitInventory` — 原価を等分し端数は最後の子、親は `split`。**過去の原価は動かない**
- **下書き仕入**：`mellojoy-watch` の `meta.json` から商品・型番・数量を積む（価格なし）。`confirmPurchase` で単価と送料を入れて在庫を生成。同じ記録は二度積まない（`import_key`）
- **販売実額**：`applySaleActuals` で手数料・送料を `shipping_source='actual'` として確定。収集は未入力の販売だけ詳細を開く（**セレクタ未検証**）。説明文の型番は `appendModelCodes` で追記し、取り込み直後の「私物」は型番が見つかれば転売に戻す
- **私物判定**：キーワード未設定なら「型番があれば転売、なければ私物」として取り込む
- **収集の振る舞い**：普通の Chrome の UA と `Accept-Language`、2〜6 秒のランダム待ち、1回 6 ページまで、既定 1 時間、CAPTCHA・本人確認で即停止
- **画面**：売上（自動チップ・実額表示・型番で自動紐付け・メモ）、在庫（型番チップ・分割・型番の手修正・メモ・分割済みフィルタ）、仕入（購入記録の取り込み・下書きの確定フォーム・型番プレビュー・メモ・仕入先の種別）、ホーム（価格未入力の仕入・型番ランキング）、設定（キーワード・記録フォルダ・取り込み先アカウント）
- テスト 77 件（按分・型番抽出・自動紐付け・分割・下書き→確定・マイグレーション・UA・CAPTCHA 判定・meta.json 読み込み）

確認: `pnpm typecheck` / `pnpm test` 緑。画面は隠しウィンドウのスクリーンショットで確認。**実機での取り込み（メルカリ詳細・メロジョイ注文履歴）は DOM 未確認のため未検証**

## 2026-09-19 実機フィードバック対応（Phase 2 続き）

- **`window.prompt()` は Electron で動かない**（「仕入先を追加」「発送方法を追加」「メモ」「分割」「型番」が無反応だった原因）。`components/InputDialog.vue` を作り、`App.vue` が `provide('prompt', ask)`。全画面の `prompt()` を置き換え
- **販売履歴ページを収集の正にした**（`/mypage/listings/sold`）。商品・価格・販売手数料・送料・購入完了日が表で取れるので、詳細ページを実額のために開かない。既知の販売も実額と本当の販売日で更新（`updateCollectedActuals`）。実DOMの抜粋を `__tests__/fixtures/mercari-sold.html` に保存。ページネーションは未対応
- **CAPTCHA 誤検知を修正**：マイページ本文に「本人確認」が普通に含まれる。URL・CAPTCHA iframe・短い本文＋キーワード に限定
- **キーワードを複数**（`,` `、` 空白区切り、どれか含めば転売）
- **mellojoy-watch 連携を撤去**（ユーザー指示）。下書き→確定の仕組みは注文履歴用に残す
- **メロジョイのログイン窓**（`collector-mellojoy.ts`、アカウント別 `persist:mellojoy-<id>`）。設定タブの「仕入先」からログイン。注文履歴の取得は DOM 未確認で未実装
- **設定タブを再構成**：手数料／在庫／取り込み／メルカリ（ログイン・キーワード）／仕入先（一覧・ログイン）／データ。ヘッダーの「メルカリにログイン」を撤去。振込手数料は「月1回の振込として月次に計上」の前提（R-05 は未実装）
- テスト 82 件
- **日本語入力の確定 Enter でダイアログが閉じていた**（「仕入先に文字が打てない」の原因）。IME 変換中（`isComposing` / keyCode 229）は反応しないように
- **取引データの初期化**（設定 → データ）：販売・仕入・在庫・紐付け・取り込み履歴・期間費用を消す。設定・仕入先・発送方法は残す。「初期化」と入力して確定
- **仕入先の登録を設定タブへ**（仕入タブのボタンは撤去）。認証は Cookie（ログイン状態）のみ保存、パスワードは保存しない方針を再確認（2026-09-19）

## 2026-09-19 自動取得の判別とタグ

- **自動取得の印**：販売の `source`（collector / manual）を画面に出す。売上タブの販売日の下に「自動取得」。紐付けの自動確定は「自動紐付け」に改名して区別
- **タグ**：`tag` / `sale_tag` / `inventory_tag`（schema_version 3）。設定タブで登録・改名・削除、売上・在庫の行の「タグ」からポップオーバー（`components/TagPicker.vue`）で付け外し（即時反映、その場で新規作成も可）。売上タブでタグ絞り込み → 件数・売上・手数料・送料・原価・粗利の合計を DB 側で集計（`saleTotals`）。CSV にも「取得元」「タグ」列
- テスト 89 件。実 DB のコピーで v1→v3 のマイグレーションを確認
- **仕入先の改名・種別変更・無効化・削除**（設定 → 仕入先）。仕入で使われている仕入先は消せず「無効にする」。削除時はログイン用プロファイルも消す（`clearShopSession`）

## 2026-09-20 メロジョイの注文履歴を取り込む（P-07）・到着状態・画面の幅とメモ

- **注文履歴の取り込み**（`collector-mellojoy.ts`）：Shopify 新カスタマーアカウントの注文一覧（`article[aria-labelledby="order-#…"]`）と注文詳細（`role="table"` の明細・注文合計）を実DOMで確認し、`__tests__/fixtures/mellojoy-orders.html` / `mellojoy-order.html` に保存。「取り込む」と起動時の自動取り込みで、メルカリの後に有効な仕入先アカウントを直列で読む（一覧1ページ＋詳細最大5件／回、2〜6秒待ち）。明細の単価が整数で決まり **小計＋配送−割引＝合計** が一致すれば確定して在庫を作る。一致しなければ取れた値を入れた下書き（`draft`）。明細が読めなければ何も作らず次回に回す。キャンセル済みは飛ばす。`import_key = mellojoy:#注文番号`。注文日は「確認日: 9月19日」（年なし）から推定。実機で 3 注文が確定（合計一致を確認）、キャンセル 1 件を除外
- **取り込みの記録**：`collector_run` に `source`（mercari / mellojoy）と `shop_account_id`（schema_version 4）。`collect()` は 1 回分の配列を返し、トーストは「メルカリ: …／アカウント名: …」にまとめる。設定の実行履歴に「対象」列
- **到着状態**（schema_version 5）：`purchase.fulfillment`（pending＝確認済み／shipped＝配達中／delivered＝配達済み）。取り込みのたびに注文一覧から既取込の注文も更新（詳細は開かない）。在庫は確定時に作り、到着まで仕入に「未発送／配送中」、在庫に「未着」を出す。紐付けは到着前でも可（ユーザー決定）。`inventory_view` に `fulfillment`
- **画面**：ページ幅を画面に追従（設定だけ 1100px）。売上・在庫・仕入の行を「商品名／状態チップ／メモ」の 3 段に組み直し（「自動取得」は販売日の下から状態チップの列へ、メモは「メモ」ラベル＋アイコン付きで本文と区別）。仕入に「自動取得」チップと代表商品名（`first_line_name` / `first_model_code`、「ほか n 点」）
- **サムネイル**（schema_version 6）：メルカリの販売履歴の画像を、新規の販売と未保存の既知の販売について 1 回の収集で最大 30 枚・300〜800ms 間隔で `userData/thumbs/<itemId>.jpg` に保存（`sale.thumb_file`）。独自スキーム `soroban-thumb://` で画面に出す（CSP に `img-src soroban-thumb:`）。売上・在庫の行頭に 40px、無ければ型番の頭文字のプレースホルダ。メロジョイ側は注文画面に画像が無い（後日、ストアの商品検索 JSON で引く案）
- **見た目を「Milestone」風の温かい帳簿へ**（ユーザーが参考画像で指名）：黄色のブランド色・白い角丸カード（罫線なし）・ベタ塗りのピル型チップ・黄色の主役カードに大きい数字（ホーム）・チャコールの主ボタン・88px の縦積みナビ・丸アイコン付きの見出し・月次の合計行。意味色（利益＝緑／損失＝赤／要対応＝橙）は維持。DESIGN.md / design.json を実装から書き直し
- 表の操作列（タグ／メモ／削除）と設定の仕入先の名前セルが `display:flex` で行から外れて描かれていたのを修正
- **OS の `confirm()` / `alert()` を全廃**（ユーザー指示「ダサい」＋ Electron でネイティブダイアログの直後にキー入力が効かなくなり、仕入先を続けて追加できなかった）。`components/ConfirmDialog.vue` と `App.vue` の `provide('confirm' | 'choose' | 'toast')` に置き換え。削除系は赤い主ボタン、成功はトースト、検証エラーは warn トースト。仕入先の追加は「名前 → 種別を 3 つのボタンで選ぶ」
- **フォントを M PLUS 2（可変ウェイト）に**（ユーザー指示）。`@fontsource-variable/m-plus-2` を同梱し `main.ts` で import（CSP は `'self'` のまま）
- 仕入先の削除時は実行記録の `shop_account_id` を外して残す
- テスト 128 件。実 DB のコピーで v3→v6 のマイグレーションを確認

## 2026-09-20 Phase 3：在庫 1 点の履歴と商品（型番）ページ

- **履歴ドロワー**（`components/TimelineDrawer.vue`、`getItemTimeline`）：注文（仕入先・注文番号・単価＋送料按分→原価）→ 仕入先が発送 → 到着 → メルカリで売れた（価格・買い手）→ 発送 → 受取 → 取引完了（売上金の反映）を時系列で。日付が無い段は「予定」。在庫・売上の行のサムネ／商品名、商品ページの在庫行から開く
- **商品タブ**（`views/Products.vue`、`listProducts` / `getProduct`）：型番ごとに 在庫数・仕入合計・平均原価・平均売価・平均粗利・粗利合計・最終仕入／販売日。詳細で月ごとの仕入数／販売数／在庫残を依存なしの SVG（`components/MiniChart.vue`）で棒＋折れ線。ホームのランキング行から飛べる
- **schema_version 7**：`purchase.shipped_at` / `delivered_at`（到着状態が変わったのを最初に観測した日。既存分は `fulfillment_updated_at` で埋める）、`sale.status` / `shipped_at` / `delivered_at` / `completed_at` / `buyer`（メルカリの取引中タブ・取引画面の DOM 待ち。まだ null）
- テスト 136 件。実 DB のコピーで v6→v7 を確認

## 2026-09-20 出品タブ・仕入タグ・全画面検索（要件 3-C）

- **出品**（schema 8）：`listing` / `listing_line`。収集のたびに出品中タブを 1 ページ読み、キーワード一致だけ upsert。出品は在庫の状態を変えず、引き当ては `listing_line` が持つ（二重引き当て・未販売以外はトリガーで拒否）。売れた販売の id と同じ出品に引き当てがあれば `sale_line`（`link_source='listing'`）へ移す。1 ページから消えただけでは解除しない
- **出品タブ**（`views/Listings.vue`、`components/AllocateDrawer.vue`）：状態・未引き当ての絞り込み、引き当てドロワー（チェックした瞬間に見込み粗利）、取り下げ。ホームの要対応に「未引き当ての出品」
- **仕入タグ・派生タグ**（schema 9）：`purchase_tag`。仕入→在庫→販売へ派生（`inherited_tags`、コピーしない）。画面では薄いチップ。タグ絞り込みは 直接 ∪ 派生
- **検索**（`components/SearchBox.vue`）：売上・在庫・仕入・商品・出品。空白 AND・NFKC。検索中は状態の絞り込みを外す
- **在庫**：未出品／出品中／販売済／その他。出品中の行に価格チップ。商品ページに「未出品 n・出品中 m」
- **守りの修正**：`linkInventory` が販売済み・廃棄済みを拒否。廃棄・分割で引き当てを外す。シリーズだけの型番（`A035`）は自動確定せず候補止まり（CLAUDE.md に明文化）
- **ペルソナ E2E**：`src/main/__tests__/scenarios/persona-01…20`（20 人の使い方を API に流して手計算で検算）。テスト 189 件
- Codex に画面構成をレビューしてもらい、「検索中は絞り込みを外す」「引き当て済みの行からも編集」「廃棄時の引き当て解除」を反映。横断検索・返品は保留

## 2026-09-20 深夜（自走）期間費用・純利益・按分の整数統一・出品タブの仕上げ

- **期間費用（R-05、schema 10）**：`expense.auto`、`expense_auto_month`。転売の販売がある月に振込手数料（設定値）を 1 件だけ自動計上（作った時点の値で固定。人が消せば再作成しない。転売が 0 件になった月からは自動行を消す）。月次に「期間費用」「純利益」列と内訳の展開（追加・削除）。ホームに「純利益」
- **送料未入力の注記**：月次の行に「送料未入力 n 件」チップと注記（送料 0 で集計されている）
- **まとめ売りの按分を整数で統一**：ビュー `sale_line_share`（1 販売の価格・粗利を点数で整数按分、端数は最後の行）。`variant_summary` / 商品ページ / 履歴ドロワーの 1 点あたりが全部これを使う。合計が販売と必ず一致（persona-02 の 1 円ずれ解消）
- **守りの修正**：引き当て中の在庫を持つ仕入の削除が FK で失敗していた → 引き当てを外してから消す。未引き当ての出品が売れたとき出品が `active` のまま残っていた → `sold` にする
- **出品タブの仕上げ**（デザインレビュー 8 件）：狭幅の折り返し・列詰め、「見込み粗利（送料前）」の明示、未引き当て行の「引き当て」を紐付けと同じ warn 枠に、「出品中」チップを info に、解除をテキストに、ヒント 13px、粗利セルの settle 遷移
- 要件 7.3（人の入力と自動取得の優先順位）を追記。テスト 194 件。実 DB のコピーで v7→v9 の移行を確認（v10 も同じ経路）

## 2026-09-20 早朝（自走）横断検索・引き当ての移動・出品の最終確認

- **横断検索**（`components/GlobalSearch.vue`、`searchAll`）：ヘッダの検索欄（Ctrl+K）で在庫／出品／売上／仕入を一度に探す。空白 AND・NFKC。結果は右ドロワーに種類ごと（状態チップ・金額・日付）。クリックで該当タブへ飛び、検索語を引き継いで該当行をハイライト。Codex 提案「あの商品どうなった？を 1 か所で」
- **引き当ての移動**：別の出品に引き当て済みの在庫も候補に出し（「出品 ¥… に引き当て済み」チップ）、チェックして「引き当てる（n 点を移す）」で付け替え。再出品・付け替えの導線（`reserveInventory` が元の引き当てを外して移す）
- **出品タブ**：「最終確認 MM/DD HH:mm」を併記。直近のメルカリ取り込みで見えなかった出品に「前回の取り込みで見えず」チップ（1 ページ目に無かっただけかもしれないので引き当ては維持）
- テスト 207 件
- **要対応の先頭に取り込みの失敗**（`DashboardStats.recentRuns`：取り込み元ごとの直近 1 件）。メルカリ／メロジョイ口座ごとに、ログインが必要・失敗・0 件 を要対応の一番上に出し、「ログインして取り込む」「もう一度取り込む」で即再実行。最後の 1 回だけ見て失敗が隠れる問題を解消（Codex 指摘 5）。ペルソナ E2E 21〜24 追加。テスト 213 件

## 2026-09-20 昼（自走）Codex コードレビューの反映（schema 11）

Codex に main 側（出品・派生タグ・期間費用・整数按分・横断検索・collector）をレビューさせ、確度の高いバグ 7 件と仕様の注意 1 件を修正。
- **P1** ホームの純利益が常に 0（`getDashboard` が `monthly_summary` を直読みしていた）→ `listMonthly` 経由に
- **P1** 終了済み（sold／ended）の出品に引き当てられ、在庫が二重に見える → トリガーと `reserveInventory` で拒否（v11 でトリガー作り直し）。画面も終了済みの行から操作ボタンを消し、ドロワーに「この出品は終了しています」
- 出品から引き継いだ販売は、キーワード不一致でも `kind='resale'`（人が引き当てた＝転売の意思）
- `listing.last_seen_at` を ISO に統一（v11 で既存行を変換）。「前回の取り込みで見えず」の判定を Date 比較に（文字列比較で常に真だった）
- 出品一覧・引き当て候補の検索を他画面と同じ NFKC・AND に（横断検索から飛ぶと一覧で消えていた）
- collector：販売 0 件でも出品中タブを読む／総数は読めるのに 0 件解析なら「構造が変わった可能性」を message に／サムネ予算（30 枚／回）は成功数ではなく試行数で減らす
- 按分の規則の表現：均等割り（分割・まとめ売り）は「余りを末尾の行に 1 円ずつ」。仕入の按分（端数は最終行）と区別して CLAUDE.md・要件に明記
- テスト 222 件。実 DB のコピーで v7→v11 を確認

## 2026-09-20 昼（2）売上＋出品の統合・サマリとグラフ・仕入先の取り込みキーワード・メルカリの除外

- **売上タブに出品を統合**（出品タブは廃止）：段階の切替 出品中 (n)｜未処理 (n)｜完了｜すべて（既定は未処理）。1 行＝メルカリの 1 商品で、出品中→売れた→完了と同じ行の形で流れる。ドロワーは 1 つ（`AllocateDrawer` の mode: listing／sale）。「すべて」は閲覧用に列を絞る
- **サマリとグラフ**（`components/SalesSummary.vue`）：今月の粗利（売上・件数・純利益）／出品中 n 件と見込み粗利／未処理 n 件。直近 12 か月の 売上（棒）・粗利・純利益（線）を 150px の SVG で。転売／私物の切替、隠す／見せる（記憶）
- **仕入先ごとの取り込みキーワード**（schema 12、`shop_account.import_keywords`）：商品名が一致する明細だけ取り込む。一致 0 件の注文は取り込まない。一部一致は一致した明細だけを仕入にし、送料・割引は注文全体の金額比で按分（例：2 明細 2,499＋2,399、送料 499 → 一致 1 明細に 255）。下書きは按分できないので注文全体の値のままメモに残す。設定の仕入先の行で編集
- **メルカリもキーワード不一致は取り込まない**（販売も。詳細・サムネも取りに行かない。除外数を run の message に）。それまでは不一致を私物として取り込んでいた
- **TikTok Shop の自動取り込みは取り下げ**（購入履歴が PC から見られない）。手動登録のまま
- テスト 232 件
