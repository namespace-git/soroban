# CLAUDE.md

そろばん（Soroban）— メルカリ販売 利益管理ツール

---

## このプロジェクトは何か

メロジョイ（Shopify）で仕入れた商品をメルカリで販売した際の、実際の利益を管理する
**単一のElectronアプリ**。ローカルSQLite。外部サービスなし（唯一の例外：レシート読み取りは利用者自身の API キーで Gemini に画像を送る。キーは safeStorage で暗号化して保存し、DB・ログ・画面に平文で出さない）。

```
src/main/      メインプロセス（DB・収集・IPC）
src/preload/   橋渡し
src/shared/    IPC契約（両側が参照する）
src/renderer/  画面（Vue 3）
```

詳細は `docs/01-requirements.md`。

---

## 最重要：この製品の核心

**仕入と販売の紐付け。** ここが壊れると原価が出ず、全機能が無意味になる。

```
粗利 = 販売価格 − 販売手数料 − 発送送料 − 梱包材費 − Σ(紐付けた在庫の按分後原価)
```

---

## 絶対に守ること

| ルール | 理由 |
|---|---|
| **金額は integer（円）で扱う** | 浮動小数点の誤差を持ち込まない |
| **メルカリへの書き込み操作を実装しない** | 読み取り専用 |
| **認証情報を保存するコードを書かない** | セッションプロファイルのみ。Gemini の API キーだけは safeStorage で暗号化して `setting.gemini_api_key_enc` に持つ（`getSettings` から除外、IPC ログにも出さない） |
| **収集頻度を上げない** | 既定1時間。1回に数ページ、ページ間 2〜6 秒。ループやリトライで連打しない |
| **取得0件を成功にしない** | `empty` として記録する。静かな欠損が最も危険 |
| **紐付けを自動確定しない** | 候補提示まで。確定は人間。例外は3つだけ。**①抽出した型番が 1 つで、在庫の `model_code` と文字列が完全一致**（枝番の有無は問わない。在庫 `A037` と出品 `【A037】` は可。在庫が `A035-1` で出品が `【A035】` なら不一致＝候補止まり。先入先出。`×2`／`2個` の個数表記があればその点数、無ければ 1 点。`link_source='auto'`）。**②タイトルに在庫コード（`【S-0012】`）があれば、その在庫を名指し**（分割品・セット用。ただし**別の出品に引き当て済みの在庫は奪わず候補止まり**）。**③人が出品に引き当てた在庫**（売れたときにそのまま引き継ぐ、`link_source='listing'`）。どれも1クリックで解除できること |
| **出品は在庫の状態を変えない** | 出品中も資産。引き当ては `listing_line` が持ち、「出品中」は派生表示。1ページから消えただけで引き当てを解除しない（未観測≠取り下げ） |
| **タグはコピーしない** | 仕入→在庫→販売へ派生で見せる（`inherited_tags`）。上流で直せば下流に効き、紐付けを解除すれば消える |
| **`inventory_item.landed_cost` を後から書き換えない** | 生成時に確定。過去の利益が動くと帳簿が信用できない |
| **手数料率を変えても既存の販売を再計算しない** | 同上 |
| **`shared/types.ts` の型を勝手に変えない** | IPC契約。変えるなら preload と main も揃える |
| **Windows / macOS の両方で動かす** | パスは `node:path`。OS判定は `process.platform` |
| **DBファイルをコミットしない** | `.gitignore` を確認 |
| **キーワードに一致しないものは取り込まない** | メルカリ（販売・出品）も仕入先（注文の明細）も、キーワードが設定されていれば一致したものだけ。不一致の分の詳細・画像も取りに行かない。空なら全部 |
| **収集は人間と同じ振る舞い** | 普通の Chrome に見える UA・言語・サイズ。CAPTCHA や本人確認が出たら止めて人に渡す。自動突破・頻度引き上げは書かない |
| **画像は「変わったときだけ」取り直す** | サムネイルは 1 回の収集で 30 枚まで。保存した元 URL（`thumb_src`、クエリ込み＝画像の更新時刻）と違うときだけ差し替える。同じなら取りに行かない |
| **販売済みの在庫の付け替えは人が警告を読んでから** | `takeFromSale` は画面の確認ダイアログの後にだけ渡す。元の販売は未紐付けに戻り要対応に出る |

---

## データモデルの要点

| テーブル | 役割 |
|---|---|
| `purchase` | メロジョイへの1注文 |
| `purchase_line` | 注文内の1商品（数量を持つ） |
| `inventory_item` | **販売可能な1点**。明細の数量分だけ生成 |
| `sale` | メルカリの1取引 |
| `sale_line` | 販売と在庫の紐付け。**1販売に複数可**（まとめ売り） |
| `listing` / `listing_line` | メルカリの出品と、人が引き当てた在庫。売れると `sale_line` に移る |
| `purchase_tag` / `inventory_tag` / `sale_tag` | タグ。下流へは派生で見える（コピーしない） |
| `expense` / `expense_line` | 経費（レシート 1 枚＝1 件、計上月 `month`）。月の明細で販売用の販売に按分する（金額＝販売価格の比／数量＝点数の比、余りは最後の行、私物には配賦しない） |
| `month_book` | 月の按分方法と締めの記録（締めた時点の数字。編集はロックしない） |
| `collector_run` | 収集の実行記録 |
| `app_log` | 操作ログ（IPC の書き込み系・収集・画面の例外）。14 日で削除。不具合報告はバックアップ 1 本で |
| `shop_alias` | レシートの登録番号 → 店名 の学習。初期化で消さない |

ビュー `sale_profit` / `monthly_summary` / `inventory_view` に計算が入っている。
レンダラー側で利益を再計算しないこと。

### 落とし穴

- **1販売＝1仕入と決め打たない。** セット販売が頻繁に起きる
- **`sale.kind` で販売用と私物を必ず分ける。** 税務上の扱いが違う
- **仕入送料は明細に按分する。** 無視すると利益が実態より大きく出る
- **`is_shipping_confirmed` が0の販売は「送料未入力」。** UI上で目立たせる
- **`sale_line.inventory_item_id` は UNIQUE。** 1つの在庫は1回しか売れない
- **売上は「実績」と「見込み」を分ける。** 実績＝取引完了（`status='completed'`）＋状態のない手入力（`source='manual' AND status IS NULL`）。判定は `shared/recognition.ts` の `isRealized` に一本化する（SQL 側にも同じ条件があるので、変えるなら両方）。月次・ホームの粗利・商品の粗利計・経費の按分は**実績だけ**。未完了は「見込み」として別に見せる
- **販売の日付は 3 つ。** `purchased_at`＝購入日時（取引画面。空の販売だけ 1 回 3 件まで開く、失敗は再試行しない）、`completed_at`＝購入完了日（販売履歴の表＝受取評価が終わり売上金が入る日）、`sold_at`＝**計上日**（月次・月フィルタの基準。取り込みの販売は完了していれば完了日、未完了は購入日時の仮置きで完了時に動く。手入力は入力した日付のまま）
- **商品（型番）の画像**は ①人がセット（`product_image`、取り込みで上書きしない）＞ ②仕入先（メロジョイの注文詳細の明細画像 `purchase_line.image_file`）＞ ③最新の出品 ＞ ④最新の販売 の順で派生。②③④は都度引く
- **仕入の到着状態の自動更新は前に進む方向だけ**（未発送→配送中→到着済）。人が「到着済」にしたものを注文一覧の「配送中」で戻さない。手動はどこへでも変えられる
- **分割前の親（status='split'）は在庫の一覧に普段は出さない**。`InventoryGroupFilter` の `split`（メンテナンス）を選んだときだけ

### 按分（`db.ts` の `allocate`）

```
配賦対象 = 仕入送料 + その他費用 − 割引
```

金額按分か数量按分で各明細へ配賦し、**端数は最終行に寄せて総額を一致させる**。
ここを丸め処理でずらすと、合計が合わなくなる。

一方、**均等割り**（在庫の分割 `splitEvenly`、まとめ売りの 1 点あたり `sale_line_share`）は
`floor(合計/点数)` を配って**余りを末尾の行に 1 円ずつ**（1,001 円÷3 → 333/334/334）。どちらも合計は必ず一致する。

---

## コマンド

```bash
pnpm dev          # 開発起動
pnpm build        # 型チェック + ビルド
pnpm typecheck    # 型チェックのみ
pnpm dist:win     # Windows インストーラ
pnpm dist:mac     # macOS dmg
```

---

## collector を触るときの注意

`src/main/collector.ts` の `scrape()` のセレクタは**未検証**。
実際のメルカリのDOMを DevTools で確認して調整する必要がある。

直すときは以下を守る。

- 商品リンク（`a[href*="/item/m"]`）を起点にする。**クラス名に依存しない**
- タイトルは `img[alt]` から取る。これが最も安定している
- 価格は正規表現で拾う。`¥1,234` と `1,234円` の両方に対応する
- 取得できなかったら `empty` か `failed` を返す。**空配列を成功として握りつぶさない**

---

## UI の優先順位

1. **ホームの「要対応」** ← 毎日ここを見る
2. **売上タブ**（送料選択・紐付け）← 毎日触る。1件10秒で終わること
3. 仕入登録
4. 在庫・月次
5. 設定

タップ数・クリック数を増やす実装は却下する。
紐付けパネルでは、チェックを入れた瞬間に粗利が見えること。

---

## 作業の進め方

1. `docs/01-requirements.md` を読む
2. 作業計画を提示して承認を得る
3. 実装する
4. `pnpm typecheck` を通す

不明点があれば推測で進めず、質問すること。
特に**金額計算・按分・紐付け**のロジックは、間違うと気づきにくいので必ず確認する。

---

## 開発の進め方（役割分担）

**親セッション（Opus）は実装しない。** 調査 → 計画 → タスク分解 → レビュー → 統合に専念する。

| 誰が | 何を |
|---|---|
| **親（Opus）** | 実地調査 → **計画** → ファイル単位に分解 → **レビュー** → 統合・コミット |
| **`implementer`（Sonnet）** | 分解済みの独立サブタスクを**実装する**。`.claude/agents/implementer.md` 参照 |

親が手を動かしてよい例外は3つだけ。

1. **`shared/types.ts` の契約変更**（IPC契約は親が先に確定させ、各 implementer に貼る）
2. **コミット・ブランチ**（implementer には触らせない）
3. **1〜2行で終わる自明な修正**（それ以上は分解して implementer へ）

### 計画は「並行で流せる形」で書く

**計画の単位は機能ではなくファイル。** 同じ波の中でファイルが重ならないように切る。
重なるなら、それは1つの WS にまとめるか、波を分ける。

### レビューは親がやる

**implementer の報告文だけで通さない。実物を見る。** `git status` / 実ファイル / `pnpm typecheck` で裏を取る。
特に**金額計算・按分・紐付け**は、合計が一致することを数字で確かめる。

### 進捗は `docs/implementation-status.md`

生きているタスクだけを置く。波の開始・終了で親が更新する。**implementer には触らせない。**
