# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主利用者：作者本人と、同居する家族・パートナー。** どちらも日々の入力をする。

- メロジョイ（Shopify）で仕入れた商品をメルカリで販売している個人。仕入先アカウントは2つ
- 規模は月20〜50取引、1日1〜2件。専業ではなく、生活の合間に扱う
- 家族・パートナーは発送方法の選択・在庫との紐付け・仕入登録まで担う。**作者の説明なしに、間違えずに操作できること**が前提
- 私物の売却も同じメルカリアカウントで混在して発生する

配布して第三者に使ってもらう予定はない（2026-09-19 時点）。

## Product Purpose

「いつ・何円で仕入れた商品が、どの販売に対応して、手数料と送料を引いて何円の利益になったか」を答える。

存在理由は、メルカリにもメロジョイにも API・CSV がなく、手計算では仕入と販売の対応が追えないこと。
成功の定義は要件書のとおり：**先月分の利益が、手計算と一致すること。**

副次的に、私物売却と販売用所得を分けて集計し、税務申告の根拠になる数字を出す。判断は代替しない。

## Positioning

- **仕入と販売の紐付けが核心。** 1在庫は1点単位、1販売に複数在庫を紐付けられる（まとめ売り前提）。ここが壊れると原価が出ない
- **紐付けは自動確定しない。** 商品名の類似度で候補を並べるまで。確定は人間。誤紐付けは気づきにくいため
- **紐付け中に粗利が即座に見える。** チェックを入れた瞬間に数字が出る
- **取得0件を異常として扱う。** 静かな欠損を最も警戒する
- 外部サービスなし。ローカルSQLite 1ファイル。認証情報を保存しない

隣接する家計簿・在庫管理アプリは、この「按分後原価を持つ1点在庫と、まとめ売り対応の紐付け」を持たない。

## Operating Context

- **端末：ノートPC（13〜15インチ）。** 外部モニタ前提にしない。一覧の表示密度は控えめに
- **場面：発送前後にさっと開く。** 長時間開く道具ではないが、数字を読む道具ではある
- **毎日の動線（1件10秒）：** 売上タブ → 発送方法を選ぶ（送料が入る）→ 紐付けを押して在庫を選ぶ（原価が入り粗利が確定）。私物なら「販売用」バッジを「私物」に切り替えて終わり
- **仕入時：** 仕入タブで注文日・仕入先・商品・単価・数量・送料を登録。数量分の在庫が生成され送料が按分される
- **月1回：** 在庫タブの月次で集計確認、設定タブでバックアップ、長期滞留在庫の確認
- **収集：** 起動時に前回から6時間以上空いていれば埋め込みブラウザで自動取り込み。初回のみ手動でメルカリにログイン。セッション切れは画面に表示される
- **OS：** Windows 10/11 と macOS 13+ の両方で使う

画面の優先順位（CLAUDE.md より）：①ホームの「要対応」 ②売上タブ ③仕入登録 ④在庫・月次 ⑤設定。
タップ数・クリック数を増やす実装は却下される。

## Capabilities and Constraints

**実装済み（Phase 1）：** 仕入登録と按分、1点在庫の自動生成、販売の取り込みと手入力、発送方法マスタからの送料計算、手数料率からの手数料計算、販売用/私物の区別、複数在庫の紐付け・解除・付け替え、類似度による候補提示、販売ごとの粗利、月次集計（販売用/私物別）、CSV書き出し、DBバックアップ、収集の実行履歴。

**未実装（Phase 2）：** 期間費用（振込手数料）の入力と純利益、梱包材費UI、Gmail経由の仕入自動登録、利益ランキング、発送方法の推定。

**技術・運用上の制約（破ってはいけない）：**
- 金額は integer（円）。浮動小数点を使わない
- メルカリへの書き込み操作をしない。読み取り専用
- 認証情報を保存しない。セッションプロファイルのみ
- 収集間隔は既定6時間。短くしない
- `inventory_item.landed_cost` は生成時に確定し、後から書き換えない。手数料率変更でも既存販売を再計算しない
- 利益はDBのビュー（`sale_profit` / `monthly_summary` / `inventory_view`）で計算する。画面側で再計算しない
- `shared/types.ts` はIPC契約。変更は preload・main と揃える
- CSP: `default-src 'self'`。外部フォント・CDNは読めない。同梱するか system font
- 起動2秒以内

**用語（要件書 §9 を正とする）：** 仕入（purchase）／明細（purchase_line）／在庫アイテム（inventory_item、販売可能な1点）／按分後原価（landed cost）／販売（sale）／紐付け（sale_line）／私物／要対応（送料未入力または未紐付けの販売）。

**未決定：** 家族・パートナーが使うときのアカウント切り替えや操作者の記録は要件にない。必要になった時点で決める。

## Brand Commitments

- 名称は **そろばん（Soroban）**。ウィンドウタイトル・インストーラ名もこれ
- ロゴ・アイコンは未作成（2026-09-19 時点）
- 文体：README・要件書・UI文言すべて簡潔な日本語の常体。「〜すること」「〜しない」の断定。装飾的な言い回しをしない
- 既存の色の意味付けは製品の約束：**利益＝緑、損失＝赤、要対応＝琥珀**。数字は桁揃え（tabular-nums）
- **見た目の方針（2026-09-19 決定、2026-09-20 更新）：業務SaaSの標準型の配置はそのまま、雰囲気は「Milestone」（資金管理SaaS）を参考にした温かい帳簿。** ユーザーが参考画像で指名。黄色のブランド色（`--brand #f5c842`）、白い角丸カード（罫線なし・柔らかい影）、ベタ塗りのピル型チップ、黄色の主役カードに大きな数字、チャコールの主ボタン、88px の縦積みナビ。意味色（利益＝緑／損失＝赤／要対応＝橙）は維持し、黄色は「注意」ではなく「ブランド」。以後の視覚判断はこの基準に従う（詳細は DESIGN.md）
- 行の見せ方（2026-09-20）：一覧の商品セルは「商品名／状態チップの列／メモ（ラベル付き）」の 3 段。幅の節約より読みやすさを優先する（ユーザー指示）。メルカリのサムネイルを行頭に出す

## Evidence on Hand

- 要件定義：`docs/01-requirements.md`
- 運用手順・トラブル対応：`README.md`
- 実装状況：`docs/implementation-status.md`、`docs/changelog/`
- 発送方法の送料初期値：`src/main/schema.sql`（改定される可能性ありと README に明記）
- 実データ：なし。作者の実際の取引データはローカルDBにのみ存在し、リポジトリに含めない
- スクリーンショット・実機での動作確認：未実施（エージェントは Electron を起動していない）
- 収集の `scrape()` セレクタ：未検証。実DOMを見るまで動作保証なし

**捏造しないこと：** 利用者の声、実績数字、他ユーザー、料金、サポート体制。いずれも存在しない。

## Product Principles

1. **紐付けが正しいことがすべてに優先する。** 速さより誤紐付けを防ぐ。候補提示まで、確定は人間
2. **1件10秒。** 毎日の入力（発送方法・紐付け）はクリック数で測る。増える提案は却下
3. **説明なしで家族が間違えない。** 作者以外が触る前提。要対応・未入力・私物の状態は見落とせない形で出す
4. **静かな欠損を許さない。** 0件・セッション切れ・送料未入力は目立たせる。「正常に見える異常」を作らない
5. **過去の帳簿は動かない。** 原価は確定値。設定変更が過去の利益を変えない

## Accessibility & Inclusion

色覚に関する特別な配慮は不要と確認済み（2026-09-19）。ただし利益・損失は色に加えて符号（＋/−）でも読めるようにしておく。
ノートPC画面での可読性を優先し、14px 未満の本文を増やさない。
